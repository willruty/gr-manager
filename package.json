{
  "name": "gr-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo para o sistema de Guindastes Ribas",
  "scripts": {
    "backend:dev": "npm run start:dev -w backend",
    "web:dev": "npm run dev -w frontend/web",
    "mobile:dev": "cd frontend/mobile && npx expo start -c --tunnel",
    "dev:all": "npm run backend:dev & npm run web:dev & npm run mobile:dev"
  }
}
